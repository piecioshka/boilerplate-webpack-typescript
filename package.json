{
    "private": true,
    "name": "test-typescript-webpack",
    "version": "0.0.0",
    "scripts": {
        "clear": "rm -rf node_modules/ typings/ dist/",
        "build": "webpack --profile",
        "watch": "webpack --watch",
        "postinstall": "typings install env~node --global --save-dev"
    },
    "devDependencies": {
        "file-loader": "^0.9.0",
        "ts-loader": "^0.8.2",
        "typescript": "^2.0.3",
        "typings": "^1.4.0",
        "webpack": "^2.0.0-beta"
    }
}
